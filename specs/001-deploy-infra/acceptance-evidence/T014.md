# T014 Acceptance Evidence — Docker Compose Config Validation

**Task**: T014 [US2] [P] docker compose config × 2 驗證
**Date**: 2026-05-11
**Branch**: 001-deploy-infra

## Test 1: prod 模式（compose.yaml）

```bash
$ docker compose -f deploy/compose.yaml config -q
[no output]
exit code: 0
```

✅ **PASS**: config -q exit 0、stderr 空

## Test 2: dev 模式（compose.yaml + compose.dev.yaml）

```bash
$ docker compose -f deploy/compose.yaml -f deploy/compose.dev.yaml config -q
[no output]
exit code: 0
```

✅ **PASS**: config -q exit 0、stderr 空

## Test 3: Service count (prod 5 services)

```bash
$ docker compose -f deploy/compose.yaml config --services
migration
new-admin-base-web
new-admin-rust-api
postgres
redis
```

Count: 5
✅ **PASS**: 5 services present

## Test 4: dev 模式 admin-base-web profile=never

```bash
$ docker compose -f deploy/compose.yaml -f deploy/compose.dev.yaml config --services
migration
new-admin-rust-api
postgres
redis
```

✅ **PASS**: new-admin-base-web 被 `profiles: ["never"]` 排除，dev 模式不含該服務

## Test 5: prod 模式對外 ports 只有 admin-base-web

```bash
$ docker compose -f deploy/compose.yaml config --format json | jq '.services[] | select(.ports) | .'
Services with ports: new-admin-base-web
Port: 8080:80

$ docker compose -f deploy/compose.yaml config --format json | \
  python3 -c "
import json,sys
d=json.load(sys.stdin)
ones_with_ports=[s for s,v in d['services'].items() if v.get('ports')]
print(ones_with_ports)
```

Output: `['new-admin-base-web']`

✅ **PASS**: prod 模式唯一對外 service = new-admin-base-web:8080

---

## Summary

- [x] Test 1 (prod config -q): PASS
- [x] Test 2 (dev config -q): PASS
- [x] Test 3 (5 services): PASS
- [x] Test 4 (dev profile=never): PASS
- [x] Test 5 (prod ports only web): PASS

**Overall**: ✅ All acceptance criteria PASS

對應 spec **US2 acceptance + SC-002**。
