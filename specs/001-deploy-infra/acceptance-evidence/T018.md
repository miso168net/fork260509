# T018 Acceptance Evidence — Project Name Isolation

**Task**: T018 [Polish] §IV #4 project name 隔離
**Date**: 2026-05-11
**Branch**: `001-deploy-infra`

## Verification: Compose Project Name Isolation

### Test 1: Prod compose project name

```bash
$ docker compose -f deploy/compose.yaml config | grep -E '^name:' | head -1
name: new-admin-root
```

✅ **PASS** — prod compose project name = `new-admin-root`

### Test 2: Dev compose project name (override)

```bash
$ docker compose -f deploy/compose.yaml -f deploy/compose.dev.yaml config | grep -E '^name:' | head -1
name: new-admin-root-dev
```

✅ **PASS** — dev compose override applies `name: new-admin-root-dev`

### Test 3: Container naming isolation (cross-stack)

```bash
$ docker container ls --format '{{.Names}}'
docsify-mdviewer-89          ← 外部 stack（與 new-admin-root 隔離）
new-admin-root-dev-postgres-1
new-admin-root-dev-redis-1
```

✅ **PASS** — dev stack 容器命名 `new-admin-root-dev-*`；跨 stack 命名前綴隔離正確

## Findings

- **Compose project name isolation**: prod stack `new-admin-root`、dev stack `new-admin-root-dev` — 避免跨環境 service 撞名
- **Container naming**: docker 按 `${PROJECT_NAME}-${SERVICE_NAME}-${INSTANCE_NUM}` 規則命名，自動達成隔離
- **Validation**: `docker compose config` 確認設定正確；`docker container ls` 確認 runtime 隔離成立

## Requirements Verification

- [x] **FR-103** (Prod): `name: new-admin-root` ✅
- [x] **FR-102** (Dev): `name: new-admin-root-dev` (via override) ✅  
- [x] **Edge case**: 同名 service 隔離 — dev prefix `new-admin-root-dev-*` 避免撞名 ✅
- [x] **Spec § Assumptions**: 「`name: new-admin-root` 在 compose 內能避免與其他 stack 同名 service 撞名」✅

## Stack Status After T018

- postgres healthy
- redis healthy
- migration Exited(0)
- Project name isolation verified (prod + dev)
