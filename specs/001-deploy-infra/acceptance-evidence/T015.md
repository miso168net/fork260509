# T015 Acceptance Evidence: contracts × 3 validation + symlink check

**Task**: T015 [US2] [P] contracts × 3 validation + symlink check  
**Feature**: 001-deploy-infra  
**Date**: 2026-05-11  
**Branch**: 001-deploy-infra  

---

## Test Results Summary

**Overall**: ✓ **ALL PASS** (13/13 tests)

| Section | Tests | Result |
|---------|-------|--------|
| (a) contracts/env-variables.md | 4/4 | ✓ PASS |
| (b) contracts/service-naming.md | 3/3 | ✓ PASS |
| (c) contracts/nginx-routes.md | 5/5 | ✓ PASS |
| (d) FR-141 symlink check | 1/1 | ✓ PASS |
| **TOTAL** | **13/13** | **✓ PASS** |

---

## (a) contracts/env-variables.md Validation

### Test (a.1): 必填變數用 `:?` 強制
**Status**: ✓ PASS

- Required vars: `POSTGRES_PASSWORD`, `REDIS_PASSWORD`, `JWT_SECRET`, `TZ`
- Expected: ≥ 4 matches of `${VAR:?...}`
- **Result**: 7 matches found
- **Evidence**: All mandatory variables enforce strict requirement with `:?` operator in deploy/compose.yaml

```bash
$ grep -cE '\$\{(POSTGRES_PASSWORD|REDIS_PASSWORD|JWT_SECRET|TZ):\?' deploy/compose.yaml
7
```

### Test (a.2): 可選變數用 `:-` 提供 fallback
**Status**: ✓ PASS

- Variables checked: `POSTGRES_DB`, `POSTGRES_USER`, `WEB_PORT`, `RUST_LOG`, `JWT_EXPIRE`
- **Result**: All 5 variables have `:-` fallback
  - ✓ POSTGRES_DB 有 :- fallback
  - ✓ POSTGRES_USER 有 :- fallback
  - ✓ WEB_PORT 有 :- fallback
  - ✓ RUST_LOG 有 :- fallback
  - ✓ JWT_EXPIRE 有 :- fallback

### Test (a.3): .env.example 不含真實 secret
**Status**: ✓ PASS

- Check: No lines matching pattern `(password|secret).*[a-zA-Z0-9]{32,}`
- **Result**: No real secrets detected in deploy/.env.example
- **Evidence**: File contains only example/placeholder values

### Test (a.4): deploy/.env 在 .gitignore
**Status**: ✓ PASS

- Check: `git check-ignore -q deploy/.env`
- **Result**: deploy/.env is properly gitignored
- **Evidence**: Sensitive environment file excluded from version control

---

## (b) contracts/service-naming.md Validation

### Test (b.1): 5 service 全存在
**Status**: ✓ PASS

Required services: `postgres`, `redis`, `migration`, `new-admin-rust-api`, `new-admin-base-web`

**Result**: All 5 services exist
  - ✓ postgres 存在
  - ✓ redis 存在
  - ✓ migration 存在
  - ✓ new-admin-rust-api 存在
  - ✓ new-admin-base-web 存在

**Evidence**: Verified via `docker compose -f deploy/compose.yaml config --services`

### Test (b.2): nginx 反代到正確 service name
**Status**: ✓ PASS

- Pattern: `proxy_pass http://new-admin-rust-api:10001`
- **Result**: nginx/default.conf correctly proxies to new-admin-rust-api:10001
- **Evidence**: Service name aligns with compose.yaml service definition

### Test (b.3): admin-net 涵蓋所有 service
**Status**: ✓ PASS

- Check: All services connected to `admin-net` network
- **Result**: All 5 services are part of admin-net
- **Evidence**: Verified via `docker compose config --format json` and Python JSON parsing

---

## (c) contracts/nginx-routes.md Validation

### Test (c.1): nginx -t 語法檢查
**Status**: ✓ PASS

- Check: `docker run ... nginx:1.27-alpine nginx -t`
- **Result**: Syntax is valid
- **Evidence**: Output confirmed "syntax is ok"
- **Note**: Used `--add-host=new-admin-rust-api:127.0.0.1` to resolve service name in validation

### Test (c.2): 路由完整性
**Status**: ✓ PASS

Required patterns:
  - ✓ `= /health` (exact health endpoint)
  - ✓ `location /api/` (API proxy location)
  - ✓ `try_files \$uri` (fallback routing)
  - ✓ `expires 30d` (caching directive)

All 4 route patterns found in deploy/nginx/default.conf

### Test (c.3): proxy_pass target 對齊
**Status**: ✓ PASS

- Pattern: `proxy_pass http://new-admin-rust-api:10001`
- **Result**: Target service name matches compose.yaml service definition
- **Evidence**: Confirmed match

### Test (c.4): CORS 紅線守住
**Status**: ✓ PASS

- Check: Negative test for `access-control` or `cors` headers
- **Result**: No CORS-related directives found in nginx config
- **Evidence**: Constitution §I principle enforced (no CORS layer in nginx)

### Test (c.5): 5 必要 header 齊全
**Status**: ✓ PASS

Required proxy headers:
  - ✓ X-Real-IP
  - ✓ X-Forwarded-For
  - ✓ X-Forwarded-Proto
  - ✓ X-Request-Id
  - ✓ Host

All 5 headers present in deploy/nginx/default.conf via `proxy_set_header`

---

## (d) FR-141 Symlink Negative Check

### Test (d.1): deploy/ 內無 symlink
**Status**: ✓ PASS

- Check: `find deploy/ -type l | wc -l`
- **Result**: 0 symlinks
- **Evidence**: No symbolic links detected in deploy/ directory tree

---

## Mapped to Spec Requirements

### US2 Acceptance Scenarios
- **SC-002** (contracts validation): All 3 contracts passed validation ✓
- **SC-003** (deployment safety): No symlinks, proper gitignore, syntax verified ✓

### Constitution Alignment
- **§I Red Lines**: CORS rule respected (no CORS headers in nginx) ✓
- **§IV Policy #5** (service isolation): All services on admin-net ✓

---

## Conclusion

All 13 validation tests **PASS**. The deploy/ configuration meets:
- Environment variable security contract (mandatory :?, optional :-)
- Service naming and networking contract (5 services, admin-net)
- nginx routing and security contract (syntax valid, CORS free, headers complete)
- FR-141 structural requirement (no symlinks)

**Status**: Ready for next feature task.
